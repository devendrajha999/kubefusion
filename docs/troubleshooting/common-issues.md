# Troubleshooting

- Pods CrashLoopBackOff: inspect env vars and DB connectivity.
- Sync status OutOfSync: run manual sync endpoint and inspect app events.
- Auth failures: verify token issuer and auth backend configuration.
